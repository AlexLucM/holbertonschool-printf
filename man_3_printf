.TH _PRINTF 3 "27 November 2025" "Holberton School" "_printf Manual"
.SH NAME
_printf \- formatted output conversion
.SH SYNOPSIS
#include "main.h"

int _printf(const char *format, ...);
.SH DESCRIPTION
The _printf() function produces output according to a format string and writes the output to standard output.

The format string is composed of zero or more ordinary characters and conversion specifications. Ordinary characters are printed unchanged.

The conversion specifications are introduced by the character %.
.SH CONVERSION SPECIFIERS
.TP
%c
Prints a single character.
.TP
%s
Prints a string of characters.
.TP
%d
Prints a decimal integer.
.TP
%i
Prints a decimal integer.
.TP
%%
Prints a percent character.
.SH RETURN VALUE
Upon successful return, _printf() returns the number of characters printed.

On error, -1 is returned.
.SH SEE ALSO
printf(3)
.SH AUTHORS
Guillaume De Chilly, Alex Ouattara
